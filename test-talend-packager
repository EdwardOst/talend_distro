set -e
set -x

script_path=$(readlink -e "${BASH_SOURCE[0]}")
script_dir="${script_path%/*}"

source ${script_dir}/talend-packager.sh


talend_packager -m "/home/eost/talend-distro/job_manifest.cfg" -g "com/talend/se/distro/packager" -a "myapp"
